import { SqlDialect, SqlQueryFields, SqlQueryParameters } from "./types";

export const generateSql = (
  dialect: SqlDialect,
  fields: SqlQueryFields,
  query: SqlQueryParameters
): string => {
  const result = "";

  return result;
};
